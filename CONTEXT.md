# LX Music — 推荐领域语言

本仓库的推荐域围绕"从当前歌曲出发的声音探索"，不是相似度匹配。本文件只收录该领域的统一词汇，不含实现细节。

## Language

### 电台与生命周期

**探索电台**:
以当前播放歌曲为起点、连续生成探索路径的模式。开启后跟随切歌，不断以新的当前歌曲开台。
_Avoid_: 相似推荐、类似的歌、电台歌单

**Run（一次开播）**:
从开台到收台之间的完整生命周期。探索距离、一句话约束与反馈以 run 为作用边界：run 内跨锚点沿用，收台即清空。
_Avoid_: 会话（指策略边界时）

**锚点 (Anchor)**:
一轮探索所围绕的起点歌曲。跟歌重锚时，旧锚点与其探索路径一并结束。

**跟歌重锚**:
用户切到本路径之外的歌曲时，电台立即结束当前锚点的探索，并以新歌为锚点重新开台，不设置播放时长门槛。同一动作同时引发队列清空语义时，以重锚为准。

**收台**:
结束一次 run 并清理其队列残留。由显式关闭电台或连续计划失败触发；切歌伴随的队列清空不构成收台（以跟歌重锚为准）；独立的"清空稍后播放"手势目前不存在，其收台语义挂起至该手势出现；暂停播放不算收台。

### 探索与路径

**探索距离 (Radius)**:
允许候选偏离锚点的幅度，由用户在 run 内调节；值越小越贴近锚点。它调的是"可漂移范围"，不是相似度目标。

**探索路径 (Path)**:
一台之内已计划与已播曲目的有序记录，按批次标注当批的探索距离与一句话约束。

**聆听弧线 (Listening Arc)**:
一台曲目的编排单元：按 hold（守住）/ deepen（深化）/ open（打开）/ turn（转向）/ land（落下）五种角色编排，构成有呼吸的连续听感，而不是若干首各自成立的"好歌"。

**同曲 (Same Song)**:
跨来源、跨 id 变体的同一首歌。去重与排除一律以同曲语义为准，不以 id 为准。

### 分析与约束

**锚点分析 (Anchor Analysis)**:
对锚点"听觉画像 + 审美解读"的一次性解读。一句话约束发生变化即作废，下一次计划重新解读锚点。

**召回方向 (Recall Directions)**:
锚点分析输出的若干条探索通道（每条带审美桥接说明），是候选来源的设计，而不是歌单。

**一句话约束 (Instruction)**:
用户对探索方向的自由文本修正（如"不要华语"），run 内持续生效。其中的明确否定是硬约束，须从召回源头换道满足，不能只靠后置过滤。

**反馈 (Feedback)**:
far（太远，收紧距离）/ good（方向正确）两档。只影响 run 内后续批次，不打断当前播放。

**红心排除**:
「我喜欢」列表仅用于把候选挡在探索之外，不构成任何正向偏好证据。

**世界断裂 (World Break)**:
候选与锚点之间不可接受的听感跳变（如原声民谣接四踩电子舞曲）。近距离探索时直接否决。

### 引擎分档

**AI 引擎 / 本地引擎**:
有无 LLM 配置对应的两档排序实现。本地引擎是完整可用的降级档，不是残缺态；无 Key 时电台照常开台。

**特征事实单 (Feature Sheet)**:
播放中实时音频信号的统计摘要，只陈述可测信号，不臆断流派与编曲。
